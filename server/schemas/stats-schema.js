const stats_schema = {
  hits: Number,
  singles: Number,
  doubles: Number,
  triples: Number,
  homeruns: Number,
  plate_appearances: Number,
  at_bats: Number,
  games: Number
}

module.exports = stats_schema